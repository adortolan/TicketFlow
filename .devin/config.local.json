{
  "permissions": {
    "allow": [
      "Exec(cd)",
      "Exec(ls)",
      "Exec(mkdir)",
      "Exec(rmdir target)",
      "Exec(Remove-Item)",
      "Exec(rm)",
      "Exec(dir)",
      "Exec(Get-ChildItem)",
      "Exec(git status)",
      "Exec(git log)",
      "Exec(git add)",
      "Exec(git commit)",
      "Exec(git push)",
      "Exec(netstat)",
      "Exec(taskkill)",
      "Exec(curl)"
    ]
  }
}